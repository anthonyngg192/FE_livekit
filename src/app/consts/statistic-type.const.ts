export enum StatisticType {
  TEXT = 'text',
  NUMBER = 'number',
  SELECT_SINGLE = 'select-single',
  SELECT_MULTIPLE = 'select-multitple',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  DATE = 'date',
}
